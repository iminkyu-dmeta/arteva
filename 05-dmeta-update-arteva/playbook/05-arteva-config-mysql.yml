---

- name: update arteva
  gather_facts: True
  vars:
    hostname: "arteva1"
    host: arteva
    ansible_python_interpreter: /usr/bin/python3
  hosts: "{{ host }}"
  become: True 

  roles:
    - role: config-mysql
